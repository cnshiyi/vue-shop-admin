# Frontend Extras Archive

This directory preserves non-primary frontend apps and playground assets that are no longer part of the active Shop admin workspace.

Archived here:
- `apps/backend-mock`
- `apps/web-antdv-next`
- `apps/web-ele`
- `apps/web-naive`
- `apps/web-tdesign`
- `playground`

Current active frontend entry remains:
- `apps/web-antd`

These archived directories are kept for reference and backup, but are no longer treated as active workspace packages or day-to-day business frontend targets.
