# Vue Shop Admin 开发红线

本仓库自动化任务以中文记录为准。任何 Codex 会话开始修改前，必须先运行 `git status --short`，确认当前用户改动，避免覆盖。

## 冻结区

- 代理列表页面和相关分组、分页、列开关、视图模式逻辑属于冻结区。
- 生命周期计划、通知计划、自动续费计划等任务页面属于冻结区。
- 除非用户明确点名要求修改上述模块，否则只允许只读排查和记录，不主动重构、优化、改样式、修文案或顺手修复。

## 通用边界

- 不改真实生产配置、密钥、部署脚本或发布流程，除非用户明确要求。
- 不打印或提交密钥、Token、Telegram session、云厂商凭据或完整代理链接。
- 涉及后端接口口径时，先核对 `/Users/a399/Desktop/data/shop` 的当前实现，不凭前端猜测修改。
